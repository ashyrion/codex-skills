interface:
  display_name: "Security Compliance Guard"
  short_description: "민감정보 유출 방지와 공개 레포 안전성을 점검"
  default_prompt: "Use $security-compliance-guard to run secret scanning and public-repo safety checks before push/release."

policy:
  allow_implicit_invocation: true
