# Layout Rules

- T04: two-panel layout (filters/list + weekday matrix editor)
- T05: date column sticky, rider columns horizontal scroll
- KPI hidden unless explicitly enabled in docs
- Holiday badges and edit disable rules must match policy
